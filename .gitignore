.env
__pycache__/
*.pyc

# Course workspaces are local and may contain private exam material.
# Production users import strategy.json through the browser instead of receiving bundled courses.
courses/
